/**
 * Public server entry for @lazarv/react-server/resources.
 *
 * Re-exports the server-side createResource (with cache invalidation)
 * and createResources.
 */
export { createResource, createResources } from "./typed-resource.jsx";
